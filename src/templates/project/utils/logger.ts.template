/**
 * Logger configuration
 */
import { pino } from 'pino';
import { LOG_LEVEL } from '../config/index.js';

// Initialize logger (writes to stderr, not stdout)
export const logger = pino({
  level: LOG_LEVEL,
  transport: {
    target: 'pino-pretty',
    options: {
      destination: 2, // stderr
    },
  },
});